#include "main.h"
/**
 * _abs - to ckech the absolute value
 * @a: is a data type
 * Return: absolute value of integer
 */
int _abs(int a)
{
	if (a < 0)
	{
	return (-a);
	}
	else
	{
		return (a);

	}
}
